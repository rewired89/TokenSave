# distill-plugin/scripts

## codemap.py
- `def first_line(doc)`
- `def summarize_python(path)` — Return list of (kind, signature, summary) for top-level defs/classes.
- `def summarize_js(path)`
- `def build_dir_map(dirpath, filenames)`
- `def main(root)`
